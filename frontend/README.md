# Resume Career Advisor Frontend

## Run locally

```bash
npm install
npm run dev
```

The upload form calls the backend at `http://localhost:8000` by default. Set `VITE_API_BASE_URL` if your API runs elsewhere.
